# Legacy static calendar

Vanilla HTML/CSS/JS version of the Travel Distribution calendar (pre–React migration).

## Run locally

```bash
cd legacy
python3 serve.py
```

Open http://localhost:3000/ (serves `travelcore-rm-hub.html`).

The active app is the Vite + React project in the repo root (`npm run dev`).
